# Проверка реализации — 9 сентября 2026

## Результат

- `npm test`: **21/21** — чистая формула, контрольные суммы, границы операций, нормализация ввода, все HTML-страницы, локальные ссылки и файлы, WOFF2, JSON-LD и отсутствие фиктивных данных.
- `node tests/browser.cjs`: **45/45** сочетаний страницы и ширины. Страницы index, privacy, consent, cookies, 404; ширины 320, 360, 390, 430, 768, 1024, 1280, 1440, 1920 px. Chromium headless. Горизонтального переполнения, незагруженных изображений и ошибок JavaScript нет.
- Интерактивная проверка: ИП → ООО → ИП, все налоговые режимы, 4 900 / 8 900 / 6 100 / 6 700 ₽, сотрудники/кадры, дополнительные услуги, пустое/отрицательное/дробное значение, кнопки плюс/минус, передача параметров в окно заявки.
- Все карточки услуг, тарифы и основные CTA, поиск, 6 FAQ. Мобильное меню на прокрученной странице, Escape, фокус, постоянная горизонтальная позиция шапки, восстановление scrollY с точностью до 1 px, якорь, переход в desktop и низкий экран 320×480.
- Формы: пустой/неверный телефон, неотмеченное согласие, фактический ответ ненастроенного PHP endpoint, сохранение ввода. Управляемые ответы теста проверили сетевую ошибку, ошибку сервера и блокировку повторного запроса.
- Cookie: принятие, повторное открытие, перезагрузка, переход между страницами и недоступный localStorage. Reduced motion сохраняет весь контент.
- `node tests/extended.cjs`: цепочка услуга → форма → возврат фокуса к карточке; закрытие крестиком и по фону; максимальные числа калькулятора без переполнения; фактическое содержимое POST с параметрами и версией согласия; отображение успеха на **смоделированном** успешном ответе; содержание, FAQ и контакты при отключённом JavaScript.
- `node tests/server.cjs`: **11** проверок endpoint — CSRF, cross-site, согласие и его версия, телефон, honeypot, длина, инъекция перевода строки, 503 без SMTP, 429 при повторе, 413 при превышении размера. Ни одного письма эти тесты не отправляли.
- PHP syntax checks для обработчика и конфигурации, JS syntax checks — без ошибок.
- `node tools/prepare-site.cjs`: подготовлен `dist`, индексация выключена, домен не подставлен. PHPMailer 7.1.1 установлен и зафиксирован composer.lock.

## Визуальная проверка

Главная вручную сопоставлена со скриншотом: порядок секций, первый экран, холодная палитра, фото, сетка 3×2, калькулятор 2 колонки, тарифы, специалист, этапы, преимущества, форма, FAQ и контакты сохранены. Заменены только неподтверждённые достижения и фото. Сняты полные скриншоты `home-1440.png`, `home-768.png`, `home-390.png`, `home-320.png`.

Исправлены статические начертания шрифтов (заменены переменными WOFF2), переполнение числовых полей калькулятора на 360 px, фокус при последовательном открытии окон. Тест закрытия ждёт завершения 200 мс анимации перед вводом в фоновый элемент.

Измеренные контрасты ключевых пар: основной текст 13,75:1, вторичный 6,39:1, бордовый/белый 11,21:1, надзаголовок 5,86:1. Это проверка перечисленных цветов, не полная сертификация доступности. Фотографии вместе с JPEG fallback и всеми вариантами занимают около 1,18 МБ.

## Не проверено и зависит от публикации

- Реальное SMTP-письмо: нет настроек и секретов. Сайт **не готов к приёму заявок по почте**, пока они не заданы и доставка не проверена.
- Домен, HTTPS и серверные заголовки Apache/Nginx, реальный обработчик 404 на хостинге: хостинг не предоставлен; встроенный PHP-сервер не применяет .htaccess.
- Физические iOS/Safari/Android-устройства и почтовые приложения: проверки выполнены в Chromium с разными viewport, не на устройствах.
- Юридические тексты, сроки и инфраструктура хранения, сведения оператора и прайс требуют согласования.
- Lighthouse/внешние SEO-оценки, поисковая индексация и нагрузочная проверка не проводились. Позиции в поиске не обещаются.
- Git-репозитория в исходной папке не было; коммит, push и публикация не выполнялись.
